# 宏观研究计划 (版本 3.0) - YOLOPX 项目

**文档版本:** 3.0
**启动日期:** 2025-07-30
**核心贡献者:** Gemini-CLI & User

---

## **1. 研究动机与核心问题 (Motivation & Core Problem)**

### **1.1 研究动机**

在自动驾驶领域，构建一个能够同时处理检测、分割等多种视觉任务的**统一感知模型**是实现高效、鲁棒环境感知的关键。多任务学习 (Multi-task Learning, MTL) 为此提供了理论框架，但实践中常常受困于**任务间冲突 (Task Conflict)** 的问题——不同任务的优化目标相互矛盾，导致梯度更新方向不一致，最终损害一个或多个任务的性能。这使得模型虽然能够“同时执行”多个任务，却无法在每个任务上都达到最优。

本项目旨在系统性地探索并解决这一核心挑战，最终目标是开发一个**任务冲突最小化**的统一视觉感知模型，使其在所有子任务上都能达到或接近 SOTA 性能，为博士论文提供坚实的理论与实验基础。

### **1.2 待解决的研究问题**

我们当前的研究并非漫无目的地探索，而是围绕一个在**初步实验 (阶段1)** 中发现的关键现象展开：

> **初步发现:** 在一个共享的骨干网络上，采用 **Anchor-based (YOLOP-style)** 的检测头比采用 **Anchor-free (YOLOPX-style)** 的检测头，表现出**显著更低（约25%）的任务冲突**。

这个反直觉的发现引出了一系列亟待解答的核心研究问题：

1.  **问题的普适性:** 这个现象是特定于我们实验中采用的 ELANNet 架构，还是在其他 YOLOP 系列的官方架构 (如 YOLOPv1, v2, v3) 中也普遍存在？
2.  **冲突的根源:** 导致任务冲突差异的根本原因是什么？是 Anchor 机制本身作为一种先验知识稳定了梯度，还是不同骨干网络 (如 CSP-Darknet vs. E-ELAN) 的特征提取能力差异所致？
3.  **现有的解决方案是否有效:** 已发表的通用任务冲突优化方法 (如 PCGrad, CAGrad 等) 在这些复杂的、专门为自动驾驶设计的模型上表现如何？它们能否无差别地解决所有架构的冲突问题？
4.  **最终的解决方案:** 我们能否基于对上述问题的深刻理解，设计出一种**新的、更优的**多任务优化方法或模型架构，使其在性能和任务平衡性上超越现有的所有基线？

---

## **2. YOLOP 家族模型演进与架构对比**

为了系统性地研究上述问题，我们必须首先对 YOLOP 家族的模型有一个清晰的认知。以下表格梳理了各个模型的演进脉络和核心架构异同。

*(注: 模型发表顺序确认为 YOLOP(v1) -> YOLOPv2 -> YOLOX -> YOLOPv3(RS)。YOLOX 是一个独立的、影响力巨大的 anchor-free 模型，YOLOPX 是我们项目中对标 YOLOX 理念的实现。)*

| 模型 | **YOLOP (v1)** | **YOLOPv2** | **YOLOPX (我们的实现)** | **YOLOPv3 (遥感)** |
| :--- | :--- | :--- | :--- | :--- |
| **核心理念** | 经典、稳健的多任务基线 | 更快、更强的效率优化 | 对标 YOLOX 的 Anchor-free 范式 | 针对遥感场景的适应性改进 |
| **解决的核心问题** | 验证一个统一模型同时处理三个驾驶任务的可行性。 | 在维持精度的前提下，大幅提升模型的**推理速度**和**训练效率**。 | 探索 **Anchor-free** 范式在多任务驾驶场景下的潜力。 | 解决遥感图像中**小目标检测**和复杂背景下的车道线提取问题。 |
| **关键技术** | CSP-Darknet + SPP + FPN/PAN | **E-ELAN Backbone** | ELANNet + **Anchor-free Head** | **SimAM 注意力** + GS-FPN |
| **检测范式** | **Anchor-based** | **Anchor-based** | **Anchor-free** | **Anchor-based** |
| **Backbone** | CSP-Darknet | **E-ELAN** | ELANNet (YOLOv7-like) | ELAN-W |
| **Neck** | SPP + FPN + PAN | SPP + FPN + PAN (优化版) | PaFPNELAN | GS-FPN, RepC3-PAN |
| **代码参考** | `/workspace/YOLOP_v1_official` | 需手动实现 | `/workspace/YOLOPX/v2` | `/workspace/YOLOP_v3_official` |

---

## **3. 分阶段研究计划 (Phased Research Plan)**

所有实验都将在我们已建立的、独立的 `YOLOPX/v2` 平台中进行。

### **阶段 1: 基线复现与冲突普适性验证**

*   **目标:** 忠实复现 YOLOP 家族的关键模型，并验证我们初步发现的“anchor-based 冲突更小”现象是否具有普适性。
*   **核心实验 1:** 对 YOLOP 系列模型进行逐一的任务冲突检测。
*   **产出:**
    1.  在 `v2` 平台中，可运行的、忠实于官方实现的 `YOLOPv1`, `YOLOPv2`, `YOLOPv3` 模型。
    2.  一份详细的冲突分析报告，定量对比这三个模型与我们现有 `YOLOPX` 在 TCI 和损失比率上的差异。

### **阶段 2: 现有优化方法的泛化性评估**

*   **目标:** 评估已发表的通用 MTL 优化方法在不同 YOLOP 架构上的表现，寻找它们的优势与短板。
*   **核心实验 2:** 选择 1-2 个代表性模型（例如，冲突最高的 YOLOPX 和冲突最低的 YOLOPv1），应用 `PCGrad`, `CAGrad`, `TAG` 等方法进行训练，并分析其对 TCI 和最终任务性能的影响。
*   **产出:**
    1.  一个重构后的、更具扩展性的 `v2` 训练框架，可以轻松地插入和切换不同的 MTL 优化器。
    2.  一份对比分析报告，评估不同优化方法在不同架构下的有效性，并尝试回答：“是否存在一种普适的最优方法？”

### **阶段 3: 创新方法提出与多类别验证**

*   **目标:** 基于前两阶段的洞察，提出我们自己的、性能更优的冲突解决方案，并在更复杂的多类别任务上进行验证。
*   **核心实验 3:**
    1.  **设计与实现:** 设计我们自己的创新方法。这可能是一种**混合式检测头**，一个**新的损失函数**，或是一种**任务感知的梯度调整策略**。
    2.  **单类别对比:** 首先在单类别设置下，证明我们的新方法在降低 TCI 和提升性能上优于所有基线模型和现有优化方法。
    3.  **多类别验证:** 将最优的模型（我们的创新模型）扩展至 BDD100K 的10类别检测任务，确保其在更复杂的场景下依然能保持低冲突和高性能。
*   **产出:**
    1.  **论文的核心贡献:** 一个新颖的、有理论支撑和实验验证的多任务冲突解决方法。
    2.  一个在 BDD100K 多类别任务上取得 SOTA 性能的、经过充分验证的模型。

### **阶段 4: 任务头扩展性探索 (可选)**

*   **目标:** 探索我们提出的方法在更广泛的多任务场景下的扩展能力。
*   **核心实验 4:**
    *   **方向一 (检测头深化):** 将多类别检测头进一步细分为更专门化的头（如交通灯头、交通标志头），探索任务粒度对冲突的影响。
    *   **方向二 (新任务引入):** 为模型增加全新的任务头，如**深度估计**，并分析新任务的加入对原有任务冲突格局的影响。
*   **产出:** 对我们方法泛化能力的有力证明，为论文的“未来工作”部分提供内容。

---

## **4. Git 分支管理策略 (Branching Strategy)**

为了确保各个实验的独立性和代码的整洁性，我们将采用以下 Git 分支策略：

*   **`main`:** 始终保持为稳定的、经过验证的 `v2` 平台代码。只接受来自 `develop` 分支的合并。
*   **`develop`:** 主开发分支，整合所有已完成并经过验证的新功能。
*   **`feat/phase1-baseline-replication`:** 用于**阶段 1** 的工作。在此分支上进行 YOLOPv1, v2, v3 的模型复现和冲突分析。完成后合并到 `develop`。
*   **`feat/phase2-optimizer-evaluation`:** 用于**阶段 2** 的工作。在此分支上重构训练器以支持不同的 MTL 优化方法。完成后合并到 `develop`。
*   **`feat/phase3-innovative-method`:** 用于**阶段 3** 的工作。在此分支上实现我们自己的创新方法。这是最大、最重要的功能分支。
*   **`feat/phase4-task-expansion`:** (可选) 用于**阶段 4** 的工作。

---

## **5. 预期研究结论与创新点 (Expected Outcome & Innovation)**

*   **预期结论:**
    1.  最终证明 anchor-based 范式确实在多任务学习中具有更低的内在冲突，并提出一个令人信服的理论解释。
    2.  证明现有的通用 MTL 优化方法并非“银弹”，它们在不同架构下的表现存在差异。
    3.  我们提出的新方法能够在多种架构和任务设置下，始终如一地降低任务冲突，并带来实际的性能提升。

*   **核心创新点:**
    1.  **系统性的实证分析:** 首次对整个 YOLOP 家族模型在多任务学习下的冲突特性进行全面的横向比较。
    2.  **理论贡献:** 提出关于“架构设计（特别是 anchor 机制）如何影响梯度动态”的新见解。
    3.  **方法论创新:** 提出一个新颖的、性能优越的、专门面向自动驾驶统一感知模型的任务冲突解决方案。

---

## **6. 开发与研究日志 (Development & Research Log)**

### **2025-07-30: 阶段 0 - 基线复现准备工作**

*   **目标:** 完成 `阶段 0` 的所有技术准备，搭建一个能够灵活加载和构建不同 YOLOP 家族模型的实验平台。
*   **状态:** ✅ **已完成**
*   **操作记录:**
    1.  **代码获取:**
        *   成功克隆 YOLOPv1 官方仓库至 `/workspace/YOLOP_v1_official`。
        *   成功克隆 YOLOPv3 官方仓库至 `/workspace/YOLOP_v3_official`。
    2.  **代码冲突分析:**
        *   对比了 `YOLOPv1` 的 `common.py` 与 `v2` 的 `common_modules.py`，发现 `Conv` 等核心模块存在实现差异 (Hardswish vs. SiLU)，确认了直接融合代码的风险。
        *   对比了 `YOLOPv3` 的 `common.py` 与 `v2` 的 `common_modules.py`，发现两者架构相似，但仍存在独有模块。
    3.  **决策 - “隔离式集成”:**
        *   基于冲突分析，最终决定采用“隔离式集成”策略，以 100% 保证官方代码的逻辑纯净性，避免任何因代码融合带来的不确定性。
    4.  **技术实现:**
        *   创建了独立的模块目录 `v2/models/yolop_v1_official_modules/` 和 `v2/models/yolop_v3_official_modules/`。
        *   将 `v1` 和 `v3` 各自的 `common.py` 完整复制到对应目录并重命名为 `modules.py`。
        *   重构了 `v2/models/builder.py`，使其能够根据 `.yaml` 配置文件中的 `model_family` 字段，智能地从正确的隔离目录中动态加载模块。
        *   创建了忠实于 `v1` 和 `v3` 官方代码的配置文件：`yolop_v1_official.yaml` 和 `yolop_v3_official.yaml`。
*   **当前结论:**
    *   `v2` 平台现在已具备同时构建和管理 `YOLOPX (v2)`, `YOLOPv1`, `YOLOPv3` 三种不同架构模型的能力。
    *   所有准备工作均已完成，可以随时启动 `阶段 1` 的核心实验。

---
**未来工作:** 日后需要文件瘦身。
