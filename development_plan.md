# 可扩展、低冲突的统一视觉感知模型 - 开发计划报告

**版本:** 1.0
**日期:** 2025-07-24

---

### 1. 研究目的

本研究的核心目标是**开发一个仅基于相机的、可扩展的、任务间冲突最小化的统一视觉感知模型**。该模型旨在通过一个高效、一体化的网络，取代当前自动驾驶系统中多个功能单一、计算资源冗余的独立模型，实现对关键驾驶场景（包括目标、车道、可行驶区域及距离）的全面、准确理解，为高级别自动驾驶提供坚实的感知基础。

### 2. 开发目的

本次开发的具体目标是，在现有YOLOPX代码库的基础上，**构建并验证一个名为“YOLOPX-V2”的、任务更复杂、功能更全面的多任务模型**。我们将通过系统性地实现、对比和分析多种冲突缓解策略，最终为该模型找到性能与效率最优的架构与训练方案。

### 3. 开发阶段

整个开发过程将遵循我们之前规划的四个研究阶段，循序渐进：

*   **阶段一：基线模型构建与冲突评估**：构建一个包含所有核心任务的、未经优化的YOLOPX-V2模型，并量化其内部固有的任务冲突程度。
*   **阶段二：梯度层面的冲突缓解**：在基线模型上，应用并评估主流的在线梯度优化算法（如PCGrad, CAGrad），从梯度层面解决任务冲突。
*   **阶段三：特征层面的冲突缓解**：引入并评估特征层面的冲突解决方法（如TAG），与梯度层面的方法进行性能对比。
*   **阶段四：系统层面的任务兼容性分析**：应用MDO论文的设计思想，对任务组合的合理性（特别是深度估计任务）进行离线量化分析，以指导最终的模型架构决策。

### 4. 现阶段状态

**当前时间**: 2025-07-24 18:45:00

**当前阶段**: **处于“阶段一：基线模型构建与冲突评估”的起始点。**

**状态分析**: 
我们当前的代码库 (`YOLOPX/`) 已经非常完善，特别是 `tools/xy_train_gemini.py` 脚本，它已经模块化地实现了阶段二和阶段三所需的大部分核心工具（如 `FixedGradientConflictSolver`），这为后续的研究提供了极大的便利。然而，当前的模型架构 (`yolopx.yaml`) 是一个相对简单的三任务、单类别模型。

因此，我们**当前的首要任务是完成阶段一的核心工作：构建功能更完整、冲突更显著的 YOLOPX-V2 基线模型**。只有在这个更复杂的模型上，后续的冲突缓解策略研究才有意义和价值。

### 5. 近期开发步骤与实现细节

为了完成阶段一，我们需要进行以下三个核心功能的开发和一次基线实验：

#### **步骤 1.1: 扩展目标检测头 (实现BBox与OBB)**

*   **核心方法论**: 扩展当前的目标检测头，使其能够同时处理常规目标的标准边界框（BBox）和如交通标志等倾斜目标的定向边界框（OBB）。
*   **核心数据流**: 
    ```
    Input -> Encoder -> Detection Head -> Sub-Head 1 (BBox) -> BBox Loss
                                     -> Sub-Head 2 (OBB)  -> OBB Loss
    ```
*   **需要实现的代码及功能**:
    1.  **修改模型配置**: 在 `lib/config/` 目录下，复制 `yolopx.yaml` 为 `yolopx-v2.yaml`。修改 `head` 部分，使其包含两个并行的检测头，一个输出BBox所需的4个参数，另一个输出OBB所需的5个参数（例如 `x, y, w, h, angle`）。
    2.  **修改损失函数**: 在 `lib/core/loss.py` 中，为OBB添加新的损失计算逻辑。除了常规的CIoU损失外，还需要计算角度损失。
    *   **核心公式**: OBB损失可以表示为 `L_obb = L_iou_rotated + L_angle`。其中 `L_angle` 可以是简单的L1或L2损失：`L1_angle = |θ_pred - θ_gt|`。
*   **期待效果**: 模型能够在一个推理步骤中，同时输出两类目标的边界框。验证时，可以分别计算BBox和OBB的mAP。

#### **步骤 1.2: 实现多类别分类**

*   **核心方法论**: 将目标检测从当前的单一类别扩展为多类别检测，以更真实地模拟自动驾驶场景。
*   **核心数据流**: 检测头的分类分支输出通道数从 `1` 变为 `num_classes`，经过Sigmoid激活函数后，与多类别的one-hot标签计算损失。
*   **需要实现的代码及功能**:
    1.  **修改模型配置**: 在 `yolopx-v2.yaml` 中，将检测头的 `nc` (number of classes) 参数从 `1` 修改为您需要的目标类别数（例如 `5`）。
    2.  **修改损失函数**: 确保 `lib/core/loss.py` 中的分类损失函数（如 `FocalLoss`）能够正确处理多类别输出。
    *   **核心公式**: 分类损失通常使用二元交叉熵损失（BCE With Logits Loss）：`L_cls = -[y*log(σ(x)) + (1-y)*log(1-σ(x))]`，其中 `y` 是真实标签，`x` 是模型输出。
*   **期待效果**: 模型能够检测并区分不同的对象类别，例如车辆、行人、交通灯等。评估指标为多类别mAP。

#### **步骤 1.3: 集成深度估计任务**

*   **核心方法论**: 在YOLOPX-V2架构中增加一个全新的解码器分支，用于像素级的深度/距离估计。
*   **核心数据流**: 
    ```
    Input -> Shared Encoder -> Depth Decoder -> Dense Depth Map
    ```
*   **需要实现的代码及功能**:
    1.  **修改模型配置**: 在 `yolopx-v2.yaml` 中，仿照分割头的结构，添加一个新的 `depth_head` 解码器。
    2.  **新增损失函数**: 在 `lib/core/loss.py` 中，增加深度估计算法中常用的损失函数，例如**尺度不变对数误差 (Scale-Invariant Logarithmic Error, SILog)**。
    *   **核心公式**: `L_depth = (1/n) * Σ(log(d_pred) - log(d_gt))^2 - (λ/n^2) * (Σ(log(d_pred) - log(d_gt)))^2`，其中 `d` 是深度值，`λ` 是一个平衡因子。
*   **期待效果**: 模型能够输出与输入图像对齐的深度图。评估指标为REL（相对误差）或RMSE（均方根误差）。

#### **步骤 1.4: 建立性能基线**

*   **核心方法论**: 将完成上述功能开发的YOLOPX-V2模型，在不使用任何高级冲突解决策略（即 `method=None`）的情况下进行完整训练。
*   **核心数据流**: 标准的训练流程，将所有任务的损失加权求和后进行反向传播。
*   **需要实现的代码及功能**:
    1.  **配置训练脚本**: 在 `xy_train_gemini.py` 中，确保可以正确加载 `yolopx-v2.yaml`，并能处理所有新任务的损失。
    2.  **数据记录与可视化**: 确保 `log_manager.py` 能够记录所有任务的性能指标和总的冲突强度指标 `tci`。
*   **期待效果**: 
    1.  得到一组**基线性能数据**（各项任务的mAP, mIoU, REL等）。
    2.  得到一条**基线冲突曲线**（`tci` 指标随训练epoch的变化图）。
    3.  这份基线报告将成为后续所有优化工作的**对照组**，用以证明后续在阶段二、三中采用的各种策略的有效性。
