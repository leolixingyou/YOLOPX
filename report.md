# Phoenix 项目重构报告

**负责人:** Gemini-CLI
**启动日期:** 2025-07-25

---

## 项目概述

本项目旨在对 YOLOPX 代码库进行一次全面的、系统性的重构。目标是简化代码结构、提升模块复用率、增强可维护性，并建立一个清晰、高效、可扩展的研究框架。整个过程将以版本控制管理，确保原始代码的完整性和重构过程的可追溯性。

---

## 任务日志

### **2025-07-25 19:00:00 - 项目启动**
- **状态:** 已启动
- **描述:** Phoenix 重构项目正式启动。

### **2025-07-25 19:15:00 - Subtask 1 完成**
- **状态:** 已完成
- **描述:** 对整个 `YOLOPX` 代码库进行了全面的信息收集和数据流分析。
- **产出:** `YOLOPX/files_flow.md` - 包含高层数据流图和核心文件功能描述。

### **2025-07-25 19:25:00 - Subtask 2 完成**
- **状态:** 已完成
- **描述:** 成功创建 `phoenix_refactor` git 分支，并将原始代码完整备份到 `v1` 目录。
- **产出:** `YOLOPX/v1/` (原始备份), `YOLOPX/v2/` (空的重构目录)。

### **2025-07-25 20:00:00 - Subtask 3 完成**
- **状态:** 已完成
- **描述:** 成功将 `v1` 的核心代码，按照 `tools/refactorying/plan.md` 的计划，精简、优化并迁移至 `v2` 目录。废弃了大量临时和无关文件，并重构了核心训练脚本 `tools/train.py` 以支持命令行配置。
- **产出:** 一个全新的、模块化的代码结构 `YOLOPX/v2/`，包含了 `cfgs`, `data`, `engine`, `models`, `mtl`, `tools`, `utils` 等清晰的子模块。

### **项目总结**
- **状态:** **已完成**
- **最终成果:**
    1.  一个全新的、遵循最佳实践的代码库位于 `YOLOPX/v2/`。
    2.  原始代码在 `YOLOPX/v1/` 中完整备份。
    3.  所有重构决策和分析都记录在 `tools/` 下的日志和计划文件中。
    4.  新的主训练脚本 `v2/tools/train.py` 现在通过命令行参数驱动，更加灵活和可扩展。
- **下一步:** **验证**。运行 `v2/tools/train.py` 并与 `v1` 的基线结果进行对比，以确保重构的正确性和数值一致性。这是项目成功的最终标准。

---
## 2025-07-29: V2版本调试与重构

### 现状

在完成了对 `YOLOPX` 项目的初步目录重构后，我们开始对 `v2` 版本的代码进行调试，目标是使其能够顺利运行。

**已完成的工作:**
1.  **目录结构清理**: `YOLOPX` 根目录结构已简化，`v1` 和 `v2` 代码分离，所有日志文件被统一移动到 `runs/` 目录下。
2.  **脚本创建**: 创建了 `scripts/conflict_solver_v1.sh` 和 `scripts/conflict_solver_v2.sh` 用于自动化执行训练流程。
3.  **V1版本调试**: 成功修复了 `v1` 版本中的一系列路径和配置错误，`conflict_solver_v1.sh` 脚本现在可以完整地运行所有方法的训练。
4.  **V2版本初步调试**:
    *   修复了 `v2/tools/train.py` 中的默认参数问题，方便直接启动。
    *   修复了数据集路径配置错误 (`v2/cfgs/data/bdd100k.yaml`)。
    *   修复了由于 Docker 共享内存不足导致的 `DataLoader` 崩溃问题（通过设置 `WORKERS: 0`）。
    *   通过对齐 `v1` 和 `v2` 的模型定义文件 (`common_modules.py`, `yolox_head.py`, `builder.py`)，解决了多个 `ImportError` 和 `AttributeError`。

### 当前遇到的核心问题

尽管我们尝试通过直接复用 `v1` 的模型定义来解决 `v2` 的架构问题，但 `v2` 在启动训练时，模型的前向传播（forward pass）过程中依旧出现 `RuntimeError: channel number mismatch` 的错误。

**错误详情:**
`RuntimeError: Given groups=1, weight of size [128, 256, 3, 3], expected input[16, 128, 48, 80] to have 256 channels, but got 128 channels instead`

**问题分析:**
这个错误反复出现，并且报错的具体层和通道数在多次修复后仍在改变。这强烈表明，`v2` 版本中**模型颈部（PaFPNELAN）**输出的特征图与**检测头（YOLOXHead）**期望的输入之间存在根本性的、难以通过简单参数修改来解决的结构性差异。

直接从 `v1` 覆盖模型定义文件到 `v2` 的策略失败了，这说明 `v2` 的**模型构建逻辑（builder.py）**或者**数据流转方式**与 `v1` 有本质不同，导致即使模块定义相同，它们被连接和使用的方式也是错误的。

### 下一步计划

与其继续在模型代码层面进行“打补丁”式的修复，不如采用更根本的解决方案：

1.  **重写 `v2` 的 `builder.py`**: 放弃当前复杂的 `v2` 模型构建器。将其完全替换为 `v1` 中更简单、已经验证可行的 `YOLOP_xy.py` 的构建逻辑。这将确保网络层之间的连接方式与 `v1` 完全一致。
2.  **统一模型配置文件**: 确保 `v2` 的 `yolopx.yaml` 在结构和参数上与 `v1` 的版本完全对齐，特别是 `PaFPNELAN` 和 `YOLOXHead` 的部分。
3.  **验证数据流**: 在代码中加入临时的打印语句（print statements），追踪 `PaFPNELAN` 输出的每个特征图的形状（shape），并与 `YOLOXHead` 的输入期望进行核对，以进行最终验证。

我们将暂停对 `v2` 脚本的运行测试，直到完成上述重构步骤。
